{
  "name": "gdrive-mcp",
  "version": "0.1.0",
  "description": "A read-only MCP server for Google Drive — search, list, and read files via any MCP client",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "gdrive-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "auth": "tsx src/index.ts auth",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "license": "MIT",
  "dependencies": {
    "@google-cloud/local-auth": "^3.0.1",
    "@modelcontextprotocol/sdk": "^1.27.0",
    "googleapis": "^144.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
